name: Schedules Execution

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: print massage
        run: echo "This is my 12pm schedule that i amdoing to automate it"
